# Использовать для сборки без сети: docker compose -f docker-compose.yml -f docker-compose.offline.yml build gateway
services:
  gateway:
    build:
      dockerfile: services/gateway/Dockerfile.offline
