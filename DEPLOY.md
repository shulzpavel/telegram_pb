# 🚀 Инструкция по деплою на сервере

## Шаг 1: Подключение к серверу

```bash
ssh your_username@your_server_ip
```

---

## Шаг 2: Клонирование/обновление репозитория

### Если репозиторий еще не клонирован:

```bash
# Перейдите в домашнюю директорию или выберите место для проекта
cd ~

# Клонируйте репозиторий
git clone https://github.com/shulzpavel/telegram_pb.git

# Перейдите в директорию проекта
cd telegram_pb

# Переключитесь на ветку main
git checkout main
```

### Если репозиторий уже существует:

```bash
# Перейдите в директорию проекта
cd ~/telegram_pb  # или путь где у вас проект

# Обновите код из GitHub
git fetch origin
git checkout main
git pull origin main
```

---

## Шаг 3: Установка зависимостей

```bash
# Убедитесь что вы в директории проекта
cd ~/telegram_pb

# Создайте виртуальное окружение (если еще не создано)
python3 -m venv venv

# Активируйте виртуальное окружение
source venv/bin/activate

# Установите зависимости
pip install --upgrade pip
pip install -r requirements.txt
```

---

## Шаг 4: Создание .env файла

```bash
# Скопируйте пример конфигурации
cp env.example .env

# Отредактируйте .env файл с реальными токенами
nano .env
```

**Заполните `.env` файл реальными значениями:**

```dotenv
# Telegram Bot Configuration
BOT_TOKEN=ваш_реальный_токен_от_botfather

# User Role Tokens
USER_TOKEN=ваш_user_token
LEAD_TOKEN=ваш_lead_token
ADMIN_TOKEN=ваш_admin_token

# Jira Configuration
JIRA_URL=https://ваш-домен.atlassian.net
JIRA_USERNAME=ваш_email@domain.com
JIRA_API_TOKEN=ваш_jira_api_token
STORY_POINTS_FIELD=customfield_10022

# Supported Topics (JSON format)
# Пример: {"-1003087077812": ["ALL"]} - разрешает все топики в чате
# Пример: {"-1003087077812": [1, 2, 3]} - разрешает конкретные топики
SUPPORTED_TOPICS={"-1003087077812": ["ALL"]}

# State File Path
STATE_FILE=data/state.json
```

**Сохраните файл:** `Ctrl+O`, `Enter`, `Ctrl+X`

**Важно:** Убедитесь что файл `.env` имеет правильные права доступа:
```bash
chmod 600 .env
```

---

## Шаг 5: Настройка systemd service

```bash
# Скопируйте service файл в systemd
sudo cp telegram-pb-bot.service /etc/systemd/system/

# Отредактируйте service файл с реальными путями
sudo nano /etc/systemd/system/telegram-pb-bot.service
```

**Обновите следующие строки в файле:**

```ini
[Service]
# Замените your_username на ваше реальное имя пользователя на сервере
User=your_username

# Замените /path/to/telegram_pb на реальный путь (например: /home/user/telegram_pb)
WorkingDirectory=/home/your_username/telegram_pb

# Замените /path/to/telegram_pb на реальный путь
EnvironmentFile=/home/your_username/telegram_pb/.env

# Замените /path/to/telegram_pb на реальный путь
# Если используете venv, путь будет: /home/your_username/telegram_pb/venv/bin/python3
ExecStart=/usr/bin/python3 /home/your_username/telegram_pb/run.py
```

**Пример правильной конфигурации:**
```ini
[Service]
User=ubuntu
WorkingDirectory=/home/ubuntu/telegram_pb
EnvironmentFile=/home/ubuntu/telegram_pb/.env
ExecStart=/usr/bin/python3 /home/ubuntu/telegram_pb/run.py
```

**Сохраните файл:** `Ctrl+O`, `Enter`, `Ctrl+X`

---

## Шаг 6: Создание директории для данных

```bash
# Создайте директорию для данных (если еще не создана)
mkdir -p ~/telegram_pb/data

# Убедитесь что у пользователя есть права на запись
chmod 755 ~/telegram_pb/data
```

---

## Шаг 7: Запуск бота через systemd

```bash
# Перезагрузите systemd для применения изменений
sudo systemctl daemon-reload

# Включите автозапуск бота при загрузке системы
sudo systemctl enable telegram-pb-bot

# Запустите бота
sudo systemctl start telegram-pb-bot

# Проверьте статус
sudo systemctl status telegram-pb-bot
```

---

## Шаг 8: Проверка работы бота

### Проверка статуса:

```bash
# Статус service
sudo systemctl status telegram-pb-bot

# Логи в реальном времени
sudo journalctl -u telegram-pb-bot -f

# Последние логи
sudo journalctl -u telegram-pb-bot -n 50
```

### Проверка в Telegram:

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте команду `/start` или `/help`
4. Проверьте что бот отвечает

---

## Шаг 9: Полезные команды для управления

### Остановка бота:
```bash
sudo systemctl stop telegram-pb-bot
```

### Перезапуск бота:
```bash
sudo systemctl restart telegram-pb-bot
```

### Просмотр логов:
```bash
# Все логи
sudo journalctl -u telegram-pb-bot

# Логи за последний час
sudo journalctl -u telegram-pb-bot --since "1 hour ago"

# Логи в реальном времени
sudo journalctl -u telegram-pb-bot -f
```

### Отключение автозапуска:
```bash
sudo systemctl disable telegram-pb-bot
```

---

## Шаг 10: Обновление бота (когда появятся новые изменения)

```bash
# Перейдите в директорию проекта
cd ~/telegram_pb

# Остановите бота
sudo systemctl stop telegram-pb-bot

# Обновите код
git pull origin main

# Активируйте виртуальное окружение
source venv/bin/activate

# Обновите зависимости (если изменились)
pip install -r requirements.txt

# Запустите бота снова
sudo systemctl start telegram-pb-bot

# Проверьте статус
sudo systemctl status telegram-pb-bot
```

---

## 🔧 Решение проблем

### Бот не запускается:

1. **Проверьте логи:**
   ```bash
   sudo journalctl -u telegram-pb-bot -n 100
   ```

2. **Проверьте права на файлы:**
   ```bash
   ls -la ~/telegram_pb/.env
   ls -la ~/telegram_pb/run.py
   ```

3. **Проверьте что .env файл существует и заполнен:**
   ```bash
   cat ~/telegram_pb/.env
   ```

4. **Проверьте что Python и зависимости установлены:**
   ```bash
   source venv/bin/activate
   python3 --version
   pip list
   ```

### Бот запускается но не отвечает:

1. **Проверьте токен бота в .env:**
   ```bash
   grep BOT_TOKEN ~/telegram_pb/.env
   ```

2. **Проверьте логи на ошибки:**
   ```bash
   sudo journalctl -u telegram-pb-bot -f
   ```

3. **Проверьте что бот не запущен в другом месте:**
   ```bash
   ps aux | grep run.py
   ```

### Ошибки с Jira:

1. **Проверьте Jira credentials в .env:**
   ```bash
   grep JIRA ~/telegram_pb/.env
   ```

2. **Проверьте что Jira API token действителен**

3. **Проверьте логи на ошибки Jira:**
   ```bash
   sudo journalctl -u telegram-pb-bot | grep -i jira
   ```

---

## ✅ Чеклист готовности к деплою

- [ ] Репозиторий клонирован/обновлен
- [ ] Виртуальное окружение создано и активировано
- [ ] Зависимости установлены (`pip install -r requirements.txt`)
- [ ] `.env` файл создан и заполнен реальными токенами
- [ ] `telegram-pb-bot.service` настроен с правильными путями
- [ ] Директория `data/` создана
- [ ] Бот запущен через systemd
- [ ] Статус бота: `active (running)`
- [ ] Бот отвечает в Telegram

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи: `sudo journalctl -u telegram-pb-bot -f`
2. Убедитесь что все пути в service файле правильные
3. Проверьте что все токены в `.env` действительны
4. Убедитесь что бот не запущен в другом месте

---

**Успешного деплоя! 🚀**




